<h1> <%= @article.title %></br >

<p> <%= @article.body %> </p>


<h2>Comments</h2>
<%= render @article.comments %> <%# This renders the partial in comments view folder once for each comment that is in the @article.comments %>


<h2>Add a comment</h2>
<%= render 'comments/form' %>

<ul>
    <li><%= link_to "Edit", edit_article_path(@article) %></li>
    <li><%= link_to "Back", articles_path %></li>
    <li><%= link_to "Delete", article_path(@article), data: {
                        turbo_method: :delete,
                        turbo_confirm: "Are you sure?"
                    } %> </li>
</ul>